<Window x:Class="WpfApp1.Sample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Sample" Height="450" Width="800">
    <!--
    以下の行を追加することで、自身をバインドする
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
    -->
    
    
    <Grid>
        <TextBlock Text="{Binding Text}" Background="Pink" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="20" FontWeight="Bold"/>


    </Grid>
</Window>
